# Optional standalone development gateway only.
# Desktop users configure optional AI inside the app; no .env is required.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash
PORT=8787
