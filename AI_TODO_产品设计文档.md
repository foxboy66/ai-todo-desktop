# AI 个性化 ToDo 产品设计文档

**文档状态**：MVP 方案初稿  
**版本**：v0.1  
**日期**：2026-09-04

## 1. 产品概述

AI 个性化 ToDo 是一款面向容易拖延、难以估算任务时间和安排日程的个人效率工具。

产品不只记录待办事项，而是参与任务从规划到完成的全过程：用户输入当天任务和可用时间，AI 生成可解释的参考计划；用户修改并确认后，系统按照任务时长设置执行检查点、收集进度，并在任务受阻或延期时先了解原因，再提出新的安排建议。

MVP 优先验证一个核心问题：这种“计划协商 + 执行跟进 + 原因优先重排”的方式，是否能帮助用户更顺利地开始并完成当天任务。

## 2. 目标用户

- 经常拖延，需要外部推动才能开始任务的用户。
- 不擅长估算耗时、安排当天任务的用户。
- 容易被临时事项打断，需要及时调整计划的用户。
- 面对笼统目标时，不知道下一步该做什么的用户。

## 3. 产品设计原则

1. **计划必须由用户确认**：AI 生成的是建议，不应未经确认直接改变用户日程。
2. **先了解原因，再调整计划**：延期后先收集实际情况，再提出重排方案。
3. **提醒服务于推进，而不是制造打断**：提醒次数根据任务时长和执行状态动态调整。
4. **保持结果可解释**：估时、排序、冲突和调整影响都应让用户看得懂。
5. **先完成基础闭环**：MVP 使用简单可靠的规则，不提前构建复杂的个性化模型。

## 4. MVP 核心流程

1. 用户输入当天任务和可用时间。
2. AI 整理任务信息、估算耗时并生成参考计划。
3. 用户修改任务、时间和顺序，确认后锁定计划。
4. 系统按照任务时长生成开始提醒、执行检查点和结束提醒。
5. 用户通过快捷选项同步进度、阻塞或中断情况。
6. 任务未按计划完成时，系统先询问原因。
7. AI 根据当前进度和剩余时间提出重排建议。
8. 用户确认后应用新计划，系统继续跟进执行。

## 5. MVP 主要功能

### 5.1 今日任务录入

- 支持自然语言批量输入任务。
- AI 将输入整理为任务名称、优先级、截止时间和预计耗时。
- 用户可以编辑 AI 整理后的字段。
- 支持手动新增、删除和调整任务顺序。

MVP 暂不支持重复任务和复杂依赖关系。

### 5.2 可用时间设置

- 支持设置当天多个可用时间段。
- 支持标记临时占用时间。
- 系统不得将任务安排到不可用时间段。

### 5.3 AI 估时与参考计划

- AI 为任务提供参考耗时，并说明主要依据或不确定性。
- 根据任务优先级、截止时间和可用时间生成当天时间表。
- 计划包含任务顺序、起止时间和适量缓冲。
- 时间不足时展示冲突和未安排任务，不静默删除任务。
- AI 估时和用户确认耗时分别保存。

### 5.4 计划修改与确认

- 用户可以修改任务耗时、顺序、优先级和可用时间。
- 修改后重新计算参考计划。
- 用户确认前，计划保持草稿状态，不启动提醒。
- 用户确认后锁定计划；后续调整仍需再次确认。

### 5.5 动态执行提醒

每个任务包括开始提醒、若干执行检查点和结束提醒。执行检查点根据用户确认的任务时长生成，首版采用简单、透明的规则：

- 30 分钟以内：通常不设置中途检查点。
- 30～90 分钟：约每 30 分钟检查一次。
- 90～180 分钟：约每 45 分钟检查一次。
- 3 小时以上：约每 60 分钟检查一次，并提示用户考虑拆分任务。

用户可以修改提醒间隔、跳过本次提醒、延后提醒，或关闭当前任务的后续中途提醒。任务耗时发生变化后，系统重新计算尚未触发的提醒。

为避免提醒造成额外打断，系统应限制提醒频率；用户未回复时不连续发送提醒。

### 5.6 进度同步

提醒出现时，用户可以快速选择：

- 正常推进
- 已完成约 25% / 50% / 75%
- 比计划快
- 比计划慢
- 被打断
- 遇到阻塞
- 需要延长时间
- 已完成

系统记录提醒时间、用户反馈、实际开始时间、实际结束时间和任务完成情况。

### 5.7 延期处理与计划重排

- 任务超时或用户反馈受阻后，系统先询问原因。
- 原因至少包括：低估耗时、临时打断、任务不清楚、缺少条件、精力不足和优先级变化。
- AI 根据剩余任务和可用时间生成调整建议。
- 调整前展示受影响的任务和时间变化。
- 用户可以接受建议、继续修改、保持原计划、延期或取消任务。
- 只有用户确认后才应用新计划。

### 5.8 轻量任务拆解

- 对笼统目标提供“帮我拆解”入口。
- 必要时询问 1～2 个关键约束。
- 将目标拆成 3～7 个可执行子任务。
- 每个子任务包含动作、预计耗时、完成标准和前置条件。
- 用户选择需要的子任务加入今日计划。

MVP 不负责生成完整长期学习路线，也不自动搜索课程、软件或素材。

### 5.9 今日时间线与基础记录

- 展示当天已完成、正在进行、即将开始和未安排的任务。
- 保存 AI 预计耗时、用户确认耗时、实际耗时和延期原因。
- 保存计划调整记录，便于后续分析。
- 提供删除全部本地数据的入口。

## 6. 状态设计

### 6.1 任务状态

`待安排 → 已安排 → 进行中 → 已完成`

任务也可以进入 `部分完成`、`已延期` 或 `已取消` 状态。任务超时但尚未收到用户反馈时，由系统标记为临时的“等待反馈”状态。

### 6.2 计划状态

`草稿 → 已确认 → 已结束`

每次用户确认重排后生成一个新的计划版本，并保留调整前后的记录。

## 7. MVP 页面组成

### 7.1 今日页

展示当天时间线、当前任务、下一项任务、任务状态和未安排任务，是用户日常使用的主页面。

### 7.2 任务与时间录入页

用于输入当天任务、设置可用时间，并查看 AI 整理后的结构化结果。

### 7.3 计划确认页

展示 AI 参考计划、估时说明、时间冲突和未安排任务，支持用户修改并确认。

### 7.4 执行提醒卡片

展示当前任务、目标、预计剩余时间和快捷进度选项，并允许延后或关闭中途提醒。

### 7.5 重排确认页

展示延期原因、当前进度、重排建议以及对后续任务的影响，由用户决定是否应用。

## 8. 数据与智能策略

MVP 只做数据记录，不训练复杂的个人模型。需要保留的核心数据包括：

- 原始任务描述和结构化任务信息。
- AI 预计耗时、用户确认耗时和实际耗时。
- 计划版本及用户修改记录。
- 提醒节点和用户进度反馈。
- 中断、阻塞和延期原因。

后续版本可以使用这些数据改进个人耗时估计和提醒策略。

## 9. 产品形态建议

MVP 建议采用本地优先的桌面端形态，以保证任务执行期间的系统通知和后台提醒更加可靠。用户数据优先保存在本地，减少账号、云同步和隐私处理带来的首版复杂度。

如果首版先采用 Web 形态，应明确页面关闭、浏览器休眠和通知权限可能导致提醒失效。

## 10. MVP 暂不包含

- 周视图和长期计划。
- 重复任务和复杂任务依赖。
- 跨天自动排程。
- 日历双向同步。
- 多设备同步和账号系统。
- 邮件、短信或第三方即时通信提醒。
- 自动获取学习课程、软件或练习素材。
- 基于历史数据的自动个性化模型。
- 高度个性化的提醒策略。
- 数据导出。

## 11. MVP 验收标准

首版至少应稳定完成以下闭环：

1. 用户输入多个任务和可用时间段。
2. AI 生成包含估时和冲突提示的参考计划。
3. 用户修改并确认计划。
4. 系统根据不同任务时长生成相应数量的执行检查点。
5. 用户通过提醒同步进度或反馈中断。
6. 系统在任务延期时先询问原因，再生成调整建议。
7. 用户确认新安排后，提醒和今日时间线同步更新。
8. 系统正确保存预计耗时、实际耗时和调整记录。

## 12. 后续迭代方向

在核心闭环验证有效后，再逐步增加：

1. 基于个人历史的耗时校准。
2. 提醒频率和提醒方式个性化。
3. 周计划、重复任务和跨天安排。
4. 日历、移动端和多设备同步。
5. 更完整的长期目标拆解和学习资源推荐。
